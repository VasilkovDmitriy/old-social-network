export const getIsAuth = (state) => state.auth.isAuth;

export const getAuthenticatedUserData = (state) => state.auth.authenticatedUserData;

export const getAuthErrorMessage = (state) => state.auth.authErrorMessage;

export const getCaptchaUrl = (state) => state.auth.captchaUrl;