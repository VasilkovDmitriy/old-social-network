export const getUsersItems = (state) => state.users.usersItems;

export const getTotalUsersCount = (state) => state.users.totalUsersCount;

export const getPortionNumber = (state) => state.users.portionNumberOfItems;

export const getPageSize = (state) => state.users.pageSize;